{
  "name": "vitalreach",
  "version": "1.0.0",
  "description": "AI-Powered Rural Healthcare Copilot",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn main:app --reload --port 8000",
    "install:all": "cd frontend && npm install"
  }
}
